# Configurações de Ambiente - UniSafe Frontend
# Copie este arquivo para .env.local e configure as variáveis

# ==========================================
# API BACKEND
# ==========================================

# URL da API backend (use localhost para desenvolvimento)
NEXT_PUBLIC_API_URL=http://localhost:5000

# Para produção, altere para a URL do seu servidor backend:
# NEXT_PUBLIC_API_URL=https://api.unisafe.com.br

# ==========================================
# INSTRUÇÕES
# ==========================================
#
# 1. Copie este arquivo:
#    cp .env.local.example .env.local
#
# 2. Para desenvolvimento local:
#    - Use NEXT_PUBLIC_API_URL=http://localhost:5000
#    - Certifique-se de que o backend está rodando na porta 5000
#
# 3. Para produção:
#    - Altere NEXT_PUBLIC_API_URL para a URL do seu backend em produção
#    - Exemplo: https://api.unisafe.com.br
#
# 4. IMPORTANTE:
#    - Variáveis que começam com NEXT_PUBLIC_ são expostas ao navegador
#    - Nunca coloque informações sensíveis (senhas, chaves secretas) aqui
#    - O arquivo .env.local está no .gitignore (não vai para o Git)
#    - Use este .env.local.example como referência
#
# ==========================================
# OBSERVAÇÕES
# ==========================================
#
# - Next.js requer reinicialização após alterar variáveis de ambiente
# - Após alterar este arquivo, execute: npm run dev
# - Em produção, configure as variáveis de ambiente no seu provedor de hospedagem
#   (Vercel, Netlify, etc.)
