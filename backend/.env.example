# Configurações do Ambiente - UniSafe Backend
# 
# Este arquivo contém as variáveis de ambiente necessárias para
# configurar o servidor. O projeto utiliza MySQL hospedado no Railway.
# 
# IMPORTANTE: Em produção, nunca commite este arquivo com dados reais!

# ==========================================
# CONFIGURAÇÕES DO BANCO DE DADOS
# ==========================================
# MySQL hospedado no Railway (Produção e Desenvolvimento)
DATABASE_URL="mysql://user:password@host.railway.app:port/database"

# Exemplo de URL do Railway:
# DATABASE_URL="mysql://root:sua_senha@mainline.proxy.rlwy.net:20818/railway"

# IMPORTANTE:
# 1. Obtenha a URL pública no painel do Railway (https://railway.app)
# 2. NUNCA commite este arquivo com credenciais reais
# 3. Use variáveis de ambiente no servidor de produção
# 4. A URL interna (railway.internal) só funciona dentro do Railway
# 5. Certifique-se de usar a porta pública (geralmente 20818 ou similar)

# ==========================================
# CONFIGURAÇÕES GERAIS DO SERVIDOR
# ==========================================

# Ambiente de execução (development, production, test)
NODE_ENV=development

# Porta do servidor (default: 5000)
PORT=5000

# Chave secreta para JWT (MUDE EM PRODUÇÃO!)
# Gere uma chave forte: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=unisafe_jwt_secret_dev_2024_super_secret_key_change_in_production

# ==========================================
# CONFIGURAÇÕES DE EMAIL (Futuro)
# ==========================================

# Configurações para envio de emails (opcional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
# CONFIGURAÇÕES DE SEGURANÇA
# ==========================================

# Salt rounds para bcrypt (default: 12)
BCRYPT_ROUNDS=12

# Tempo de expiração do token JWT (7 dias)
JWT_EXPIRES_IN=7d

# ==========================================
# CONFIGURAÇÕES DE UPLOAD (Futuro)
# ==========================================

# Tamanho máximo de upload em MB
MAX_UPLOAD_SIZE=10

# Diretório de uploads
UPLOAD_DIR=./uploads

# ==========================================
# INSTRUÇÕES PARA CONFIGURAÇÃO
# ==========================================

# 1. Copie este arquivo para .env: cp .env.example .env
# 2. Configure a DATABASE_URL com as credenciais do Railway
# 3. (Opcional) Gere um JWT_SECRET forte para produção
# 4. Execute 'npm run dev' para iniciar o servidor
# 
# O banco de dados MySQL será inicializado automaticamente.
# As tabelas são criadas na primeira execução.

# ==========================================
# OBSERVAÇÕES IMPORTANTES
# ==========================================
# - O MySQL é hospedado no Railway (sem instalação local necessária)
# - Dados persistem entre diferentes máquinas de desenvolvimento
# - Backup automático diário fornecido pelo Railway
# - Para desenvolvimento local, use a mesma URL do Railway
# - Não é necessário instalar MySQL localmente

# ==========================================
# EXEMPLO DE PRODUÇÃO
# ==========================================
# Em produção, use valores mais seguros:
# 
# NODE_ENV=production
# JWT_SECRET=sua_chave_super_secreta_e_aleatoria_aqui
# DB_PASSWORD=senha_forte_producao
