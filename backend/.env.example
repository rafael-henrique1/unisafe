# Configurações do Ambiente - UniSafe Backend
# 
# Este arquivo contém as variáveis de ambiente necessárias para
# configurar o servidor e banco de dados. 
# 
# IMPORTANTE: Em produção, nunca commite este arquivo com dados reais!

# ==========================================
# CONFIGURAÇÕES GERAIS DO SERVIDOR
# ==========================================

# Ambiente de execução (development, production, test)
NODE_ENV=development

# Porta do servidor (default: 5000)
PORT=5000

# Chave secreta para JWT (MUDE EM PRODUÇÃO!)
JWT_SECRET=unisafe_jwt_secret_dev_2024_super_secret_key

# ==========================================
# CONFIGURAÇÕES DO BANCO DE DADOS MYSQL
# ==========================================

# Host do banco MySQL (localhost para desenvolvimento)
DB_HOST=localhost

# Porta do MySQL (default: 3306)
DB_PORT=3306

# Nome do banco de dados
DB_NAME=unisafe_db

# Usuário do MySQL
DB_USER=root

# Senha do MySQL (deixe vazio se não tiver senha no desenvolvimento)
DB_PASSWORD=

# ==========================================
# CONFIGURAÇÕES DE EMAIL (Futuro)
# ==========================================

# Configurações para envio de emails (opcional)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# ==========================================
# CONFIGURAÇÕES DE SEGURANÇA
# ==========================================

# Salt rounds para bcrypt (default: 12)
BCRYPT_ROUNDS=12

# Tempo de expiração do token JWT (7 dias)
JWT_EXPIRES_IN=7d

# ==========================================
# CONFIGURAÇÕES DE UPLOAD (Futuro)
# ==========================================

# Tamanho máximo de upload em MB
MAX_UPLOAD_SIZE=10

# Diretório de uploads
UPLOAD_DIR=./uploads

# ==========================================
# INSTRUÇÕES PARA CONFIGURAÇÃO
# ==========================================

# 1. Instale o MySQL em seu computador
# 2. Crie um banco de dados chamado 'unisafe_db':
#    CREATE DATABASE unisafe_db CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
# 
# 3. Se necessário, crie um usuário específico:
#    CREATE USER 'unisafe_user'@'localhost' IDENTIFIED BY 'sua_senha_aqui';
#    GRANT ALL PRIVILEGES ON unisafe_db.* TO 'unisafe_user'@'localhost';
#    FLUSH PRIVILEGES;
#
# 4. Ajuste as configurações DB_USER e DB_PASSWORD acima
# 
# 5. Execute 'npm run dev' para iniciar o servidor em modo desenvolvimento

# ==========================================
# EXEMPLO DE PRODUÇÃO
# ==========================================
# Em produção, use valores mais seguros:
# 
# NODE_ENV=production
# JWT_SECRET=sua_chave_super_secreta_e_aleatoria_aqui
# DB_HOST=seu_host_mysql_remoto
# DB_USER=usuario_producao
# DB_PASSWORD=senha_forte_producao
